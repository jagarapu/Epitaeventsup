<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Events for Thursday(16th March 2017) ONLY FOR Engineering 2 Students.{% endblock %}</title>
        {% block stylesheets %}
        <link href="{{ asset('bundles/eventsevents/css/style.css') }}" type="text/css" rel="stylesheet"/>
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}
            <p>Logged in as <b>{{ app.user.username }}</b>({{app.user.email}})<a style="padding-left: 50px;" href="{{path('fos_user_security_logout')}}">Logout</a></p>
              <div id="main">
{% block content %}
    <p>Please register for the events to be held on <b>Thursday(16th March 2017)</b></p>
    <ul>
        <li>You must choose to attend the three events held during the day.</li>
    </ul>
    {% for flashMessage in app.session.flashbag.get('error') %}        
    <div class="flash-error">
        {{ flashMessage }}
    </div>
    {% endfor %}
    <div class="candidatecontent">            
    <div id="form">                         
     <br>
     <form action="{{ path('eventthree') }}" method="post" {{ form_enctype(form) }} name="eventthree">
            <table>       
                    {# Event type 5 #}
                    <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype5) }}</b></div></td>
                        <td>{{ form_widget(form.eventtype5) }}</td>
                        <td><span class="errors">{{ form_errors(form.eventtype5) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>  
                    {# Event type 6 #}
                   <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype6) }}</b></div></td>
                        <td>{{ form_widget(form.eventtype6) }}</td>
                        <td><span class="errors">{{ form_errors(form.eventtype6) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr> 
                    {# Event type 7 #}
                    <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype7) }}</b></div></td>
                        <td>{{ form_widget(form.eventtype7) }}</td>
                        <td><span class="errors">{{ form_errors(form.eventtype7) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>  
            </table>            
            <table>
                <tbody>
                    <tr>
                        <td style="padding-left: 50px;padding-top: 10px;"><input type="submit" value="Submit" id="submit"></td>
                    </tr>
                </tbody>
            </table>
            
{{ form_end(form) }}            
            </div>         
             </div>
            {% endblock content %}   
            </div>
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
